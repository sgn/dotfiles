partial alphanumeric_keys
xkb_symbols "basic" {
	include "us"
	name[Group1] = "English (US, space stripped)";

	key <I248> { [ Control_R ] };
	replace key <SPCE> { [ space, nobreakspace ] };

	include "ctrl(nocaps)"
	include "shift(both_capslock)"
};
