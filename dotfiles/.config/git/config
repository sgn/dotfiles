[user]
	name = Doan Tran Cong Danh
	email = congdanhqx@gmail.com
	signingkey = 2FC0C6C002D75D4E

[alias]
	st = status --short
	di = diff -M -p --stat
	diw = di -w
	diww = diw --word-diff=color
	diwwd = diww --word-diff-regex=.
	dc = di --cached
	dcw = diw --cached
	dcww = diww --cached
	dcwwd = diwwd --cached
	dcc = dc --check
	ci = commit
	cia = commit --amend --no-edit
	co = checkout
	ll = ls-files
	llm = ls-files -m
	lg = log --oneline --graph
	ls-origin = for-each-ref --format='%(refname:lstrip=3)' refs/remotes/origin
	rs = restore
	ss = show -s
	s = show -M -p --stat
	shw = s -w
	shww = shw --word-diff=color
	shwwd = shww --word-diff-regex=.
	summary = show -s --date=short --pretty='format:%h, (%s, %ad)'
	sw = switch
	env = !env | grep ^GIT_
[rerere]
	enabled = true
	autoupdate = true
[pull]
	rebase = true
[push]
	default = current
[grep]
	lineNumber = true
[log]
	decorate = short
	abbrevCommit = true
[diff]
	colorMoved = true
[status]
	showStash = true
[mailinfo]
	scissors = true
[sendemail]
	smtpserver = smtp.gmail.com
	smtpencryption = ssl
	smtpuser = congdanhqx@gmail.com
	confirm = always
[credential]
	helper = pass
[index]
	recordEndOfIndexEntries = true
[gpg]
	program = gpg2
[format]
	signOff = true
[tag]
	gpgSign = true
